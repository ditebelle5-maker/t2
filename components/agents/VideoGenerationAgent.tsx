import React from 'react';

// Este componente foi removido e não é mais utilizado na aplicação.
const VideoGenerationAgent: React.FC = () => {
  return null;
};

export default VideoGenerationAgent;
